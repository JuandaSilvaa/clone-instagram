import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  styleButton: {
    width: "100%",
    height: 40,
    borderRadius: 5,
    alignItems: "center",
    justifyContent: "center",
    marginTop: 14,
  },

  text: {
    color: "#FFFFFF",
    fontSize: 10,
    fontWeight: "bold",
  },
});
