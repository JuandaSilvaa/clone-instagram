import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    marginTop: 14,
    backgroundColor: "#EEE",
    width: "100%",
    height: 40,
    borderRadius: 5,
    paddingLeft: 10,
    fontSize: 10,
  },
});
